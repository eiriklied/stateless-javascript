// Generated by CoffeeScript 1.9.3
(function() {
  var _onScroll, registeredSelectors;

  registeredSelectors = {};

  _onScroll = function(e) {
    var callback, results, selector;
    results = [];
    for (selector in registeredSelectors) {
      callback = registeredSelectors[selector];
      if ($(e.target).is(selector)) {
        results.push(callback(e));
      } else {
        results.push(void 0);
      }
    }
    return results;
  };

  window.addEventListener('scroll', _onScroll, true);

  window.stateless = window.stateless || {};

  window.stateless.onScroll = function(selector, callback) {
    return registeredSelectors[selector] = callback;
  };

}).call(this);
